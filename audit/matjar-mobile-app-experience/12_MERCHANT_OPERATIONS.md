# 12 — Merchant operations (mobile)

## Inbox
One screen per sector primary flow, reached from the العمليات tab. Filters as chips, search, status segments. Already card-based for orders; the recent decision/work split is the pattern to extend.

## Quick actions
Context-aware, from the existing module registry: add product · add service · block time · add booking · add customer · create offer. A FAB only if it does not collide with the tab bar — otherwise a row on the home screen.

## Tables still to convert
`inventory-manager`, `crm-manager`, `suppliers-manager` render desktop tables. Convert to priority-column cards: the two fields that drive a decision stay visible, the rest expand.
