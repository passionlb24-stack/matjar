# 08 — Store & offering pages (mobile)

## Store page
Order: cover (3:1, honoured everywhere since the banner work) → identity + verified → rating / area / open-now → sector quick facts → **sticky section tabs** → content → reviews → policies → about → map.

Tabs are sector-driven, e.g. `المنتجات · المعلومات · التقييمات` or `الخدمات · الأطباء · المعلومات · التقييمات`.

## Product page
Already has: gallery, price, discount, variants, quantity, reviews, bought-together, and a **sticky mobile buy bar** (shipped, needs one look on a real phone).

Remaining: variant selection should move from inline controls to a **bottom sheet** when a product has more than ~6 options.

## Food
Menu needs sticky category tabs and fast section jumping. Modifiers belong in a bottom sheet. Per-item notes already supported by the data model (`order_items.note`).
